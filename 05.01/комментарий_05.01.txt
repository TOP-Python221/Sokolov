  ====================  легенда  ====================

Верно      — всё хорошо и комментариев не требует
Замечания  — в целом верно, но можно лучше / комментарии
Ошибки     — что-то или всё пошло не так
Неверно    — условие задачи неверно интерпретировано
Нет ответа — файл с решением задачи отсутствует



  =====================  задачи  ====================

1.1. Ошибки: 
        а) в этой задаче нет неверно заданного диапазона: введённое число либо попадает в диапазон, либо нет
     Замечания: 
        а) здесь уместно использовать логический оператор or для объединения двух условий – это позволит избежать дублирования кода:
        
            if -30 < a <= -2 or 7 < a <= 25:
                print("Принадлежит")
            else:
                print("Не принадлежит")
                
        б) отсутствует вывод выполненного скрипта

1.2. Нет ответа

1.3. Замечания: 
        а) для этой задачи не нужны регулярные выражения
        б) для работы с регулярными выражениями используется модуль re; внутренняя группа модулей sre используется модулем re
        в) отсутствует вывод выполненного скрипта

1.4. Замечания: 
        а) отсутствует вывод выполненного скрипта

2.1. Замечания: 
        а) отсутствует вывод выполненного скрипта

2.2. Замечания: 
        а) проанализируйте, какие переменные сколько раз используются, и в каких действительно есть нужда в контексте данной задачи:
        
            result = 0
            for _ in range(int(input('numbers: '))):
                if (n := int(input())) > 0:
                    result += n
        
        б) отсутствует вывод выполненного скрипта

2.3. Замечания: 
        а) не слишком оптимально перебирать все числа до number включительно, т.к. делитель никогда не может быть больше number // 2
        б) отсутствует вывод выполненного скрипта

2.4. Замечания: 
        а) отсутствует вывод выполненного скрипта

2.5. Ошибки: 
        а) данная задача решает без списков; подумайте, какой тип данных можно ещё использовать?
     Замечания: 
        а) отсутствует вывод выполненного скрипта

2.6. Замечания: 
        а) по умолчанию шаг числовой последовательности, которую возвращает range(), и так равен единице – нет необходимости его явно указывать
        б) отсутствует вывод выполненного скрипта

2.7. Замечания: 
        а) аналогично 2.2
        б) отсутствует вывод выполненного скрипта

2.8. Ошибки: 
        а) при вводе единицы программа печатает два числа
     Замечания: 
        а) вывод можно делать и в цикле, надо только понять, где именно
        б) отсутствует вывод выполненного скрипта



В целом, если сами писали код, то неплохо.

Обязательно запускайте код и проверяйте на разных входных данных!
Думайте о разных вариантах входных данных, на чём ваша программа может "споткнуться".



  =====================  баллы  =====================

1.1. 1/2
1.2. 0/2
1.3. 2/3
1.4. 3/3
2.1. 2/2
2.2. 2/2
2.3. 3/4
2.4. 2/2
2.5. 1/4
2.6. 3/3
2.7. 3/3
2.8. 3/5